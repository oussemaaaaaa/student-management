apiVersion: v1
kind: Service
metadata:
  name: spring-service
spec:
  type: NodePort
  selector:
    app: spring-app   # 👈 DOIT MATCHER LE DEPLOYMENT
  ports:
    - port: 8089
      targetPort: 8089
      nodePort: 31942

